// src/types.ts
export interface User {
  id: number;
  user_id?: number;
  email: string;
  name: string;
  department?: string;
  student_number?: string;
  studentId?: string;
  birth?: string;
  birth_date?: string;
  profile_picture?: string;
}
